Read the last commits since the last npm version update.

Summarize core features and update the main Readme.md.

Don't add redundant features and don't change feature wordings just for the sake of it.

Focus on clarity and usefulness.

After updating or skipping this step use

"npm version patch && npm publish --access public" to publish the package to npm.